# Session Preferences

## File revisions

When creating a new revision of a project output file in Drive, name the new file with a "-v" + number postfix (e.g. "-v2", "-v3") instead of reusing the exact same filename. Applies to all Cowork sessions. Makes it easier to spot and delete the old version.